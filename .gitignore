# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store

# Environment variables
.env
.env.local
.env.production
.env.development.local
.env.test.local
.env.production.local
backend/.env
backend/serviceAccountKey.json
scripts/serviceAccountKey.json:1
scripts/serviceAccountKey.json
.env.*.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
serviceAccountKey.json
service-account-key.json

# Claude Code per-machine settings — the .local suffix marks this
# as a local override; tracking it in the repo conflicts with that
# convention. Permission allowlists, env vars, etc. belong to the
# machine, not the project.
.claude/settings.local.json
